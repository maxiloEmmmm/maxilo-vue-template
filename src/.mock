// 导入后端API
import { TEST } from './api/test';
import mock_set from './mock_data/draft.js';

let mocks = {...mock_set};

mocks[TEST.MOCK] = {
    /* default false */
    reg: false,
    /* data or function */
    build: {status: 0},
    type: 'post',
    title: '测试请求'
};

export default mocks;
